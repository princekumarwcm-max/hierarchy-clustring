# hierarchy-clustring
Hierarchical clustering is an unsupervised learning method that groups similar data points into clusters based on their distance or similarity. It builds a hierarchy of clusters visualized using a dendrogram.
